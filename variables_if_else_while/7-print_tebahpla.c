#include <stdio.h>

/**
 * main - Imprimir el alfabeto a reves JAJAAJA
 *
 * Return: I'm sure is 0 but I don't understand so much...
 *
 */
int main(void)
{
	char c;

	for (c = 'z'; c >= 'a'; c--)
	{
		putchar(c);
	}

	putchar('\n');

	return (0);
}
